---
# vars file for casalibs-src
casacore_version: "r{{casacore_googlecode_svn_rev}}"
casacore_install_path: "{{casalibs_legacy_install_prefix}}/casacore-{{casacore_version}}"
casarest_install_path: "{{casalibs_legacy_install_prefix}}/casarest-r{{casarest_svn_rev}}_with-core-{{casacore_version}}"
pyrap_install_path: "{{casalibs_legacy_install_prefix}}/pyrap-r{{pyrap_svn_rev}}_with-core-{{casacore_version}}"
casalibs_apt_deps: "{{ lookup('file', 'casalibs-apt-deps.txt').split() }}"